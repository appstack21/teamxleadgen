enum TextFieldType {
  number,
  string,
  email,
}

enum TextFieldFocus { name, email, mobileNumber }

enum ErrorType {
  minAge,
  maxAge,
  minRetirementAge,
  maxRetirementAge,
  minLifeExpectancy,
  maxLifeExpectancy,
  invalidAge,
  invalidRetirementAge,
  invalidLifeExpectancy,
  maxRetirementCorpusReturn,
  maxLoanPeriodMonth,
  maxLoanInterestRate,
  maxLoanPeriodYear,
  maxPeriodMonths,
  maxPeriodYears,
  maxInterestRate,
  maxReturnRate,
  maxStepUpRate,
  maxInflationRate,
  invalidWithdrawalAmount,
}
